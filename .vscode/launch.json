{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Ejecutar el script: test",
            "request": "launch",
            "command": "npm run test",
            "cwd": "${workspaceFolder}"
        }
    ]
}